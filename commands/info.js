module.exports = {
    name: 'info',
    description: '',
    execute(message, args) {
        message.channel.send('bot coded by SaberStrike. nothing else really.')
    }
}
//client.commands.get('').execute(message, args)